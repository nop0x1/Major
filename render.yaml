services:
  - type: web
    name: freefire-mock
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mock_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
